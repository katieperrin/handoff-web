export const metadata = {
  title: 'Apply for Early Access | The Handoffs',
  description: 'Apply to join the first cohort of The Handoffs — luxury bag rental and earning, by application only.',
};

import ApplyPage from './page-client';

export default function Apply() {
  return <ApplyPage />;
}
